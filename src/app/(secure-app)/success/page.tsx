// src/app/(secure-app)/success/page.tsx
// Structural placeholder for the App Router secure zone.
// The live success flow remains in src/pages/voucher-success.tsx (Pages Router).
import { redirect } from 'next/navigation';

export default function SuccessPage() {
  redirect('/voucher-success');
}
